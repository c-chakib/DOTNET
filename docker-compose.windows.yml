version: '3.7'
services:
 app:
 build: .
 image: gestionbudgetwinforms:latest
 container_name: gestionbudget_app
 # Use host networking or host.docker.internal for connecting to host MySQL
 environment:
 - DEFAULT_CONNECTION=Server=host.docker.internal;Port=3306;Database=BudgetDB;Uid=root;Pwd=chakib;
 # Note: Windows containers cannot use Linux mysql container on the same daemon easily.
 # Start the container and ensure MySQL is accessible from host.
